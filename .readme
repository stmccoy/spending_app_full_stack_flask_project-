To Run program

Run 'psql -d spending_tracker -f db/spending_tracker.sql'

database should exist but if it doesn't create it by saying createdb spending_tracker then run above command again 

then run python3 console.py 

then flask run 

change user by moving number in session file between '1', '2' and '3' 